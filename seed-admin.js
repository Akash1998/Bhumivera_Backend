const bcrypt = require('bcrypt'); const pool = require('./config/db'); async function seed() { try { const hash = await bcrypt.hash('Anritvox#*@2026', 10); await pool.query('INSERT INTO admin_users (email, password_hash) VALUES (?, ?)', ['adminwarehouse2026', hash]); console.log('Admin created: adminwarehouse2026'); } catch (e) { console.error('Error:', e.message); } finally { process.exit(); } } seed();
